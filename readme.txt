hmark - for creating hashed bookmarks (still working on it)

  example usage:

  hmark -s key=url

  bookmarks you create will be stored in the ~/.hmark/bookmarks file, where
  tilde, is of course, your home directory.
